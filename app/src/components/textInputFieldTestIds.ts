export const textInputFieldLabelTestId = (fieldName: string) => `text-input-field-label-${fieldName}`;

export const textInputFieldInputTestId = (fieldName: string) => `text-input-field-input-${fieldName}`;

export const textInputFieldErrorTestId = (fieldName: string) => `text-input-field-error-${fieldName}`;
